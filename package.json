{
  "name": "biome-simulation",
  "version": "1.0.0",
  "description": "A simple biome simulation",
  "main": "dist/index.js",
  "scripts": {
    "build": "cp -r ./assets/* ./dist && cp index.html ./dist",
    "start": "tsc && node dist/index.js",
    "lint": "eslint . --ext .ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Roger Vieira",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "http-server": "^14.1.1",
    "phaser": "^3.70.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "eslint": "^8.56.0",
    "typescript": "^5.3.3"
  }
}
