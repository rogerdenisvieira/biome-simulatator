export default class Subject {

    private radius: number;
    private health: number;
    private speed: number;


    constructor(radius: number, health: number, speed: number) {
        this.radius = radius
        this.health = health
        this.speed = speed
    }
}